[project]
# List of environment variables to be provided by each user to use the app.
user_env = []

# Duration (in seconds) during which the session is saved when the connection is lost
session_timeout = 3600

# Duration (in seconds) of the user session expiry
user_session_timeout = 1296000  # 15 days

# Enable third parties caching (e.g., LangChain cache)
cache = false

# Whether to persist user environment variables (API keys) to the database
persist_user_env = false

# Whether to mask user environment variables (API keys) in the UI with password type
mask_user_env = false

# Authorized origins
allow_origins = ["*"]

[features]
# Process and display HTML in messages
unsafe_allow_html = false

# Process and display mathematical expressions
latex = false

# Autoscroll new user messages at the top of the window
user_message_autoscroll = true

# Automatically tag threads with the current chat profile
auto_tag_thread = true

# Allow users to edit their own messages
edit_message = true

# Allow users to share threads
allow_thread_sharing = false

[features.slack]
reaction_on_message_received = false

# Authorize users to spontaneously upload files with messages
[features.spontaneous_file_upload]
    enabled = true
    accept = ["*/*"]
    max_files = 20
    max_size_mb = 500

[features.audio]
    enabled = false
    sample_rate = 24000

[features.mcp]
    enabled = false

[features.mcp.sse]
    enabled = true

[features.mcp.streamable-http]
    enabled = true

[features.mcp.stdio]
    enabled = true
    allowed_executables = [ "npx", "uvx" ]

[UI]
# Name of the assistant
name = "KBC Quiz Master"

# Set dark theme for that classic KBC studio look
default_theme = "dark"

# Wide layout for better quiz experience
layout = "wide"

# Keep sidebar open to show quiz progress
default_sidebar_state = "open"

# Description of the assistant
description = "Kaun Banega Crorepati - Test your knowledge and win virtual prizes!"

# Chain of Thought display mode
cot = "hidden"

# Custom CSS for KBC theme
custom_css = "/public/kbc-theme.css"

# Alert style
alert_style = "modern"

# Custom meta information
custom_meta_url = "https://kbc-quiz.example.com"

[meta]
generated_by = "2.8.4"